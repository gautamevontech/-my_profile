<% if !@doc.errors.empty? %>
  <div class="alert alert-error">
    <ul>
      <% @doc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="well">
  <%= form_for @doc, html: { multipart: true } do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :attachment %>
    <%= f.file_field :attachment %>
    <%= f.submit "Save Doccument", class: "btn btn-primary" %>
  <% end %>
</div>